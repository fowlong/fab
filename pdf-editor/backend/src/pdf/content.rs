//! Minimal content stream helpers.
//! Currently provides stubs that will be expanded in future iterations.

pub struct ContentStream;

impl ContentStream {
    #[allow(dead_code)]
    pub fn new() -> Self {
        ContentStream
    }
}
