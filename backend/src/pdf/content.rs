/// Placeholder module for PDF content stream parsing utilities.
///
/// The full implementation will provide tokenisers and writers for PDF
/// operators so that the backend can rewrite content streams without
/// rasterising them. For the initial scaffolding we keep this file minimal to
/// avoid unused code warnings.
pub struct ContentStream;
