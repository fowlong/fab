{
  "name": "pdf-editor-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "imports": {
    "vitest": "./tests/vitest-shim.mjs"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "NODE_PATH=tests node --loader ./tests/loader.mjs --test tests/**/*.spec.ts"
  },
  "dependencies": {
    "fabric": "^6.0.0-beta.20",
    "pdfjs-dist": "^4.2.67"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.0.0",
    "vitest": "^1.5.0"
  }
}
